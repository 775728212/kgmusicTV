# —— Flutter 构建产物 ——
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.iml

# —— 本地配置（含本机 flutter.sdk / sdk.dir 绝对路径，不应进仓库）——
android/local.properties

# —— IDE ——
.idea/
*.swp
.DS_Store
